export type ProjectsType = {
    id: number,
    title: string,
    description: string
}

export type AdvantageInfo = {
    id: number,
    title: string
}

export type StaticInfo = {
    id: number,
    title: string,
    description: string
}

export type ServicesItem = {
    id: number,
    src: string,
    title: string,
    description: string,
    icon: string
}
